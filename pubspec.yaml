name: electric_icons
description: Custom electric network icon font for Flutter applications.
version: 1.0.0
homepage:

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  fonts:
    - family: ElectricIcons
      fonts:
        - asset: lib/fonts/ElectricIcons.ttf